using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Defaults : Quantum
{
    // Start is called before the first frame update
    void Start()
    {
        base.addQuantum(5);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
